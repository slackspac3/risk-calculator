name: Deploy Test PoC

on:
  push:
    branches:
      - test-poc
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: github-pages-site
  cancel-in-progress: false

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  validate_app:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

      - name: Setup Node
        uses: actions/setup-node@v6
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
        with:
          node-version-file: .nvmrc

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: App integrity gate
        run: npm run qa:app

  publish:
    runs-on: ubuntu-latest
    needs: validate_app
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

      - name: Prepare static test site
        run: |
          mkdir -p _site
          cp index.html _site/index.html
          cp README.md _site/README.md
          find . -maxdepth 1 -type f -name 'google*.html' -exec cp {} _site/ \;
          cp -R assets _site/assets
          cp -R data _site/data
          touch _site/.nojekyll

      - name: Publish test site to gh-pages/test
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: _site
          target-folder: test
          clean: true
          force: false
          commit-message: Publish test PoC from test-poc
